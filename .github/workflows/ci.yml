name: ci

on:
  push:
    branches: [main]
  pull_request:


permissions:
  contents: read
jobs:
  test:
    name: ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

      - uses: dtolnay/rust-toolchain@29eef336d9b2848a0b548edc03f92a220660cdb8 # stable as of 2026-03-27
        with:
          toolchain: stable
          targets: wasm32-wasip1
          components: rustfmt, clippy

      - name: Install dependencies (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y bash jq shellcheck

      - name: Install dependencies (macOS)
        if: runner.os == 'macOS'
        run: |
          brew install bash jq shellcheck

      - name: Print versions
        run: |
          bash --version | head -n1
          jq --version
          shellcheck --version | head -n3

      # showy-quota requires Homebrew bash on macOS, but the macOS image's
      # default /bin/bash is 3.2. Force-run make with the Homebrew bash from
      # brew install above by prepending its bin to PATH.
      - name: Set PATH (macOS Homebrew bash)
        if: runner.os == 'macOS'
        run: |
          BREW_PREFIX=$(brew --prefix)
          echo "$BREW_PREFIX/bin" >> "$GITHUB_PATH"

      - name: make lint
        run: make lint

      - name: make test
        run: make test

      - name: cargo fmt
        run: cargo fmt --all -- --check

      - name: cargo clippy
        run: cargo clippy --workspace --all-targets -- -D warnings

      - name: cargo test
        run: cargo test --workspace

      - name: cargo audit
        if: runner.os == 'Linux'
        run: |
          cargo install cargo-audit --locked
          cargo audit

      - name: make plugin
        run: make plugin

      - name: check plugin exports
        run: |
          python3 - <<'PY'
          from pathlib import Path

          wasm = Path("target/wasm32-wasip1/release/showy-quota-zellij.wasm")
          data = wasm.read_bytes()
          assert data[:8] == b"\0asm\x01\0\0\0", "not a WebAssembly module"

          def uleb(offset):
              value = 0
              shift = 0
              while True:
                  byte = data[offset]
                  offset += 1
                  value |= (byte & 0x7F) << shift
                  if byte < 0x80:
                      return value, offset
                  shift += 7

          exports = set()
          offset = 8
          while offset < len(data):
              section_id = data[offset]
              offset += 1
              section_size, offset = uleb(offset)
              section_end = offset + section_size
              if section_id == 7:
                  count, offset = uleb(offset)
                  for _ in range(count):
                      name_len, offset = uleb(offset)
                      name = data[offset:offset + name_len].decode()
                      offset += name_len
                      offset += 1
                      _, offset = uleb(offset)
                      exports.add(name)
              offset = section_end

          required = {"_start", "load", "update", "render", "plugin_version"}
          missing = required - exports
          assert not missing, f"missing WASM exports: {sorted(missing)}"
          PY
